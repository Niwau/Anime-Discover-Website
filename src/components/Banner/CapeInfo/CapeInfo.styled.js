import styled from 'styled-components';

export const Container = styled.div`

    color: var(--white);

    h1 {
        font-size: 3.5rem;
    }

    h2 {
        font-size: 2rem;
        font-weight: 300;
    }

    p {
        margin-top: 20px;
        font-size: 1.5rem;
        font-weight: 300;
    }
`