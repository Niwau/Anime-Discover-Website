import styled from "styled-components";

export const Container = styled.div`
    height: 550px;
    background: var(--blue);
    display: flex;
    justify-content: flex-end;
    align-items: center;
`